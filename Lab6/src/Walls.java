public interface Walls {
//setting a default values for walls which we will call upon in the main class
    int DEFAULT_NUM_WALLS = 4;
    int getWalls();
//setting walls to int i
    void setWalls(int i);
}
