public interface Units {
    //setting a default values for units which we will call upon in the main class
    int DEFAULT_NUM_UNITS = 40;
    int getUnits();
    //setting units to int i
    void setUnits(int i);
}
